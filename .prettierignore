node_modules
.next
build
dist
coverage
playwright-report
test-results
pnpm-lock.yaml

# Hand-authored prose — deliberate structure (ADR format, tables) that
# Prettier's markdown reflow would fight rather than preserve.
*.md
